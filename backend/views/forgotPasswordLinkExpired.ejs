<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reset Link Expired</title>

    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family:
          system-ui,
          -apple-system,
          BlinkMacSystemFont,
          "Segoe UI",
          Roboto,
          Oxygen,
          Ubuntu,
          Cantarell,
          "Open Sans",
          "Helvetica Neue",
          sans-serif;
      }

      body {
        min-height: 100vh;
        background: linear-gradient(135deg, #f5f7fa, #c3cfe2);
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .card {
        background: #ffffff;
        max-width: 420px;
        padding: 32px 28px;
        border-radius: 12px;
        text-align: center;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.12);
      }

      .icon {
        font-size: 48px;
        margin-bottom: 16px;
      }

      h1 {
        font-size: 24px;
        color: #222;
        margin-bottom: 12px;
      }

      .message {
        font-size: 15px;
        color: #555;
        margin-bottom: 10px;
      }

      .sub-message {
        font-size: 14px;
        color: #666;
        margin-bottom: 24px;
      }
    </style>
  </head>
  <body>
    <div class="card">
      <div class="icon">⏰</div>

      <h1>Reset Link Expired</h1>

      <p class="message">
        The password reset link you clicked has expired or is no longer valid.
      </p>

      <p class="sub-message">
        Please go back and click <strong>“Forgot Password”</strong> again to
        receive a new reset link.
      </p>
    </div>
  </body>
</html>
